.TH TEST 1L "GNU 쉘 유틸리티" "FSF" \" -*- nroff -*-
.SH 이름
test \- 화일 유형을 점검하고 값을 비교한다.
.SH 개요
.B test
[표현식]
.br
.B test
{\-\-help,\-\-version}
.SH 설명
이 맨페이지는 GNU 버전의
.BR test 
를 다룬다.
대부분의 쉘은 같은 이름, 같은 기능의 내장 명령을 지니고 있을 것이다.
.PP
.B test
조건 표현식의 평가에 따라 0 (참) 또는 1 (거짓)의 상태를 반환한다.
.IR expr .
은 단항식(unary) 또는 이항식(binary)가 될 수 있다. 단항식은
보통 화일의 상태를 조사하는데 사용된다.
문자열 연산잔와 수치 비교 연산자도 있다.

.PD 0
.TP
.B \-b \fI화일\fP
만약 \fI화일\fP 이 존재하며 블럭 장치이며 참.
.TP
.B \-c \fI화일\fP
만약 \fI화일\fP 이 존재하고 문자 장치이면 참.
.TP
.B \-d \fI화일\fP
만약 \fI화일\fP 이 존재하고 디렉토리이면 참.
.TP
.B \-e \fI화일\fP
만약 \fI화일\fP 이 존재하면 참.
.TP
.B \-f \fI화일\fP
만약 \fI화일\fP 이 존재하고 보통의 화일이면 참.
.TP
.B \-g \fI화일\fP
만약 \fI화일\fP 이 존재하고 set-group-id 이면 참.
.TP
.B \-k \fI화일\fP
만약 \fI화일\fP 이 ``sticky'' 비트 설정을 가지고 있으면 참.
.TP
.B \-L \fI화일\fP
만약 \fI화일\fP 이 존재하고 심복릭 링크이면 참.
.TP
.B \-p \fI화일\fP
만약 \fI화일\fP 이 존재하고 명명된 파이프이면 참.
.TP
.B \-r \fI화일\fP
만약 \fI화일\fP 이 존재하고 읽기 가능이면 참.
.TP
.B \-s \fI화일\fP
만약 \fI화일\fP 이 존재하고 0 보다 큰 크기를 갖으면 참.
.TP
.B \-S \fI화일\fP
만약 \fI화일\fP 이 존재하고 소켓이면 참.
.TP
.B \-t [\fIfd\fP]
만약
.I fd
가 터미널 상에서 오픈된 것이면 참.
만약
.I fd
가 생략되면 기본값은 1 ( 표준출력 )이다.
.TP
.B \-u \fI화일\fP
만약 \fI화일\fP 이 존재하고 set-user-id 비트 설정을 가지면 참.
.TP
.B \-w \fI화일\fP
만약 \fI화일\fP 이 존재하고 쓰기가능이면 참.
.TP
.B \-x \fI화일\fP
만약 \fI화일\fP 이 존재하고 실행가능이면 참.
.TP
.B \-O \fI화일\fP
만약 \fI화일\fP 이 존재하고 유효 사용자 ID의 소유이면 참.
.TP
.B \-G \fI화일\fP
만약 \fI화일\fP 이 존재하고 유효 그룹 ID의 소유이면 참.
.TP
\fI화일1\fP \-\fBnt\fP \fI화일2\fP
만약 \fI화일1\fP 이 (수정일에 의거하여)
\fI화일2\fP보다 최근에 생겼다면 참.
.TP
\fI화일1\fP \-\fBot\fP \fI화일2\fP
만약 \fI화일1\fP 이 화일2보다 오래된 것이면 참.
.TP
\fI화일1\fP \fB\-ef\fP \fI화일2\fP
만약 \fI화일1\fP 과 \fI화일2\fP 이 같은 장치, 같은 아이노드 번호를 갖는다면
참.
.TP
.B \-z \fI문자열\fP
만약 \fI문자열\fP의 길이가 0 이면 참.
.TP
.B \-n \fI문자열\fP
.TP
\fIstring\fP
.I 문자열
의 길이가 0 이 아니라면 참.
.TP
\fI문자열1\fP \fB=\fP \fI문자열2\fP
두 문자열이 같으면 참.
.TP
\fI문자열1\fP \fB!=\fP \fI문자열2\fP
두 문자열이 같지 않으면 참.
.TP
.B ! \fI표현식\fP
.I 표현식
이 거짓이면 참.
.TP
\fI표현식1\fP \-\fBa\fP \fI표현식2\fP
.I 표현식1
과
.I 표현식2
가 둘 다 참이면 참.
.TP
\fI표현식1\fP \-\fBo\fP \fI표현식2\fP
.I 표현식1
또는
.I 표현식2
둘 중 하나라도 참이면 참.
.TP
.I 인수1 \fBOP\fP 인수2
.SM
여기서
.B OP
는 다음 중 하나이다.
.BR \-eq ,
.BR \-ne ,
.BR \-lt ,
.BR \-le ,
.BR \-gt ,
또는
.BR \-ge .
이러한 수치 이항 연산자들은 각각 만약 \fI인수1\fP이 \fI인수2\fP보다 같거나,
같지 않거나, 작거나, 작거나 같거나, 크거나, 크거나 같을 때 참을 반환한다.
.I 인수1
와
.I arg2
는 양의 정수, 음의 정수 또는 
.IR 문자열
의 길이를 평가하는
\fB\-l\fP \fI문자열\fP 표현식이 될 수 있다.
.SS 옵션
GNU
.B test
가 단 한 개의 인수로 시작하면 다음 옵션이 인식된다:
.TP
.I "\-\-help"
표준출력으로 사용법을 출력하고 정상적으로 종료한다.
.TP
.I "\-\-version"
표준출력으로 버전정보를 출력하고 정상적으로 종료한다.
.SH 번역자
.br
이 만 용
.B <geoman@nownuri.nowcom.co.kr>
.br
.B "         <freeyong@soback.kornet.nm.kr>"

